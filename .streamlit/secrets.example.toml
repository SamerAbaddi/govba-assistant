# Example only — do not place a real key in this file.

OPENAI_API_KEY = "replace_with_your_project_api_key"

# Generative AI configuration.
OPENAI_MODEL = "gpt-5-mini"

# Keep false until API billing, key storage, and testing are ready.
AI_ENABLED = false

# Semantic-retrieval embedding configuration.
OPENAI_EMBEDDING_MODEL = "text-embedding-3-small"

# Embedding traffic is independently disabled by default.
EMBEDDINGS_ENABLED = false
